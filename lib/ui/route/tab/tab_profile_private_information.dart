import 'package:flutter/material.dart';

class TabProfilePrivateInformation extends StatelessWidget {
  const TabProfilePrivateInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
        children: [
          Text('개인정보처리방침 내용 작성')
        ],

    );
  }
}
