import 'package:flutter/material.dart';

class TabProfilePrivateUse extends StatelessWidget {
  const TabProfilePrivateUse({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
        children: [
          Text('이용약관 작성')
        ],

    );
  }
}
